class PreparePrime < Numeric

	def is_prime?
		#check if a single number is prime
	end
	
	def next_prime
		#loop until we find a prime number here and return it to prepare_primes
	end
	
	def prepare_primes(limit)
	#prepare an array of prime numbers, equal to the limit
		
	end
end